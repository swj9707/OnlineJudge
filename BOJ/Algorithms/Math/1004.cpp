#include <bits/stdc++.h>

using namespace std;

int main(){
    ios::sync_with_stdio(0);
    cin.tie(0);
    int T;
    cin >> T;
    for(int testCase = 0; testCase < T; testCase++){
        int x1, y1, x2, y2;
        int N;
        cin >> x1 >> y1 >> x2 >> y2 >> N;
        for(int i = 0; i < N; i++){
            int cx, cy, cr;
            cin >> cx >> cy >> cr;
            //int dist_A = 
        }

    }
}