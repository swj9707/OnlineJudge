#include <iostream>

using namespace std;

int main(){
    ios::sync_with_stdio(0);
    cin.tie(0);
    int N, S; cin >> N >> S;
    vector<int> vec(N);
    for(int i = 0; i < N; i++) cin >> vec[i];
    for(int len = 1; len <= N; i++){
        int l = 0, r = len - 1;
        int sum = 0;
        
    }
}