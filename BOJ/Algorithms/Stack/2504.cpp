#include <iostream>
#include <string>
#include <stack>
using namespace std;

int main(){
    ios::sync_with_stdio(0);
    cin.tie(0);
    string tmp;
    stack<char> stk;
    getline(cin, tmp);
    int answer = 0;
    
    return 0;
}