#include <iostream>
#include <vector>
#define MAX 50
using namespace std;

int N, K, result;
string word[MAX];
bool visit[26];

void count(int alpha, int count){
    if(count == K){
        int tmp = 0;
    }
}