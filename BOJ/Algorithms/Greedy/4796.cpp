#include <iostream>

using namespace std;

int main(){
    int L, P, V;
    //캠핑장을 연속하는 P일 중 L일 동안만 사용 가능함. 
    //강산이는 이제 막 V일 짜리 휴가를 시작함
    while(L != 0 && P != 0 && V != 0){
        cin >> L >> P >> V;
        
    }
}