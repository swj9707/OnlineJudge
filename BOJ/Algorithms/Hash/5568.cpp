#include <iostream>
#include <vector>
#include <unordered_set>
#include <string>
using namespace std;

unordered_set<string> result;

int main(){
    int N, K;
    cin >> N >> K;
    vector<int> vec(N);
    for(int i = 0; i < N; i++) cin >> vec[i];
    
}