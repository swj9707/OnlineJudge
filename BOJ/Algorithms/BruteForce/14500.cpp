#include <iostream>
#include <vector>
#define MAX 500
using namespace std;

int dx[4] = {1, -1, 0, 0};
int dy[4] = {0, 0, 1, -1};
int N, M;
int MATRIX[MAX][MAX];
bool Visit[MAX][MAX];

void DFS(int x, in)

int main(){
    cin >> N >> M;
    for(int i = 0; i < N; i++){
        for(int j = 0; j < M; j++){
            cin >> MATRIX[i][j];
        }
    }
}