#include <iostream>
#define MAX 12
using namespace std;

int Arr[MAX];
int oper[4];
int main(){
    ios::sync_with_stdio(0);
    cin.tie(0);
    int N;
    for(int i = 1; i <= N; i++) cin >> Arr[i];
    for(int i = 0; i < 4; i++) cin >> oper[i];
}